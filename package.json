{
  "name": "barda-ecrivain-bible",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter barda-ecrivain-bible dev",
    "start": "npx barda-ecrivain-bible --ui"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "onnxruntime-node",
      "protobufjs"
    ]
  }
}
